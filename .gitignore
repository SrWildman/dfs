# Google Sheets credentials - NEVER commit these!
credentials.json
*credentials*.json
dfs-uploader-*.json
config.json

# Claude
.claude/

# Downloads
downloads/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Tool caches (pytest/ruff already drop their own .gitignore inside these,
# but an explicit rule here means that still works even if that file is
# ever deleted)
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv isn't this project's package manager (see README's `pip install -e
# ".[dev]"` setup) -- if a lockfile shows up locally it's incidental, not
# meant to be tracked.
uv.lock

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# New CLI rebuild
data/
config.toml

# Personal notes/strategy -- not for the public repo
docs/HANDOFF.md
docs/ROADMAP.md
docs/V2_PLAN.md
